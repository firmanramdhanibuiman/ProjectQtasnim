const colors = ['red', 'green', 'blue' , 'black'  , 'yellow'];

const entries = colors.entries();

for (const entry of entries) {
  console.log(entry);
}

// Metode dalam JavaScript digunakan untuk mengambil iterator dari pasangan kunci-nilai dalam objek seperti Array atau Map. Ini menghasilkan nilai yang dapat diiterasi untuk mengakses entri-entri dalam objek tersebut.//
