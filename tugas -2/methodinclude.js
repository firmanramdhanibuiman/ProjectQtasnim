const numbers = [1, 2, 3, 4, 5];
const includesThree = numbers.includes(5);
console.log(includesThree); 

// Metode ini ada pada objek Array di JavaScript dan digunakan untuk memeriksa apakah suatu nilai ada dalam array atau tidak. Metode ini mengembalikan nilai true jika nilai tersebut ada dalam array, dan false jika tidak.//