1. git config
Salah satu perintah git yang paling banyak digunakan adalah git config, yang bisa digunakan untuk mengatur konfigurasi tertentu sesuai keinginan pengguna, seperti email, algoritma untuk diff, username, format file, dll. Contohnya, perintah berikut bisa digunakan untuk mengatur email:
Contohnya :
git config --global user.email sam@google.com 

2. git init
Perintah ini digunakan untuk membuat repositori baru. 
Contohnya:
git ini

3. git add
Perintah git add bisa digunakan untuk menambahkan file ke index. Contohnya, perintah berikut ii akan menambahkan file bernama temp.txt yang ada di direktori lokal ke index
Contohnya :
git add temp.txt

4. git clone
Perintah git clone digunakan untuk checkout repositori. Jia repositori berada di remove server, gunakan
Contohnya :
git clone alex@93.188.160.58:/path/to/repository
Jika salinan repositori lokal ingin dibuat, 
Gunakan:
git clone /path/to/repository

5. git commit
Perintah git commit digunakan untuk melakukan commit pada perubahan ke head. Ingat bahwa perubahan apapun yang di-commit tidak akan langsung ke remote repository. 
Gunakan:
git commit –m “Isi dengan keterangan untuk commit” 

6. git status
Perintah git status menampilkan daftar file yang berubah bersama dengan file yang ingin di tambahkan atau di-commit. 
Gunakan:
git status

7. git push
git push adalah perintah git dasar lainnya. Push akan mengirimkan perubahan ke master branch dari remote repository yang berhubungan dengan direktori kerja Anda. 
Misalnya:
git push origin master

8. git checkout
Perintah git checkout bisa digunakan untuk membuat branch atau untuk berpindah diantaranya. Misalnya, perintah berikut ini akan membuat branch baru dan berpindah ke dalamnya
Gunakan :
command git checkout -b <nama-branch>
Untuk berpindah dari branch satu ke lainnya, 
Gunakan:
git checkout <branch-name>

9. git remote
Perintah git remote akan membuat user terhubung ke remote repository. Perintah berikut ini akan menampilkan repository yang sedang dikonfigurasi
Gunakan :
git rmote -v
Perintah ini membuat user bisa menghubungkan repository lokal ke remote server 
Contohnya :
git remote add origin <93.188.160.58>

10. git branch
Perintah git branch bisa digunakan untuk me-list, membuat atau menghapus branch. Untuk menampilkan semua branch yang ada di repository, 
Gunakan:
git branch
Untuk menghapus branch,
Gunakan :
git branch -d <branch-name>


Perintah git untuk kembali ke commit sebelumnya : 
Jika ingin kembali ke commit sebelumnya karena commit terakhir Anda memiliki masalah, Anda dapat menggunakan perintah git reset atau git revert, tergantung pada situasinya.
Menggunakan git reset (Hati-hati!):Jika Anda belum mendorong commit tersebut ke repositori yang bersifat bersama (remote repository) dan hanya ingin mengubah sejarah lokal, Anda dapat menggunakan git reset. 
Perintah ini mengubah sejarah commit di dalam branch.
git reset --hard HEAD~1

Perintah di atas akan menghapus commit terakhir Anda dan mengembalikan HEAD ke commit sebelumnya. Namun, hati-hati saat menggunakan --hard karena perintah ini akan menghapus perubahan yang belum di-commit.

Menggunakan git revert:
Jika Anda sudah mendorong commit ke repositori bersama (remote repository) dan tidak ingin mengubah sejarah commit, tetapi ingin membuat commit baru yang membatalkan perubahan yang diperkenalkan oleh commit sebelumnya, 
Anda dapat menggunakan git revert
git revert HEAD
Perintah di atas akan membuat commit baru yang membatalkan perubahan yang diperkenalkan oleh commit terakhir. Anda dapat menambahkan opsi -n setelah revert jika ingin mengumpulkan beberapa perubahan untuk satu commit baru.
